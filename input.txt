push float(0)
push float(1)
sub
pop
exit

; Modulus only works on integer
; push double(42.3;) fonctionne alors qu'il devrait pas

;push int8(0)
;push double(2)
;mod
;dump
;pop
;exit = segfault
; le assert qui marche pas ne stoppe pas le code
; push int8(1.1) est valide
; push float ou double(1)
